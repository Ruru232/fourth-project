/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['fakestoreapi.com'],  // Add external domain for image optimization
  },
  };
  
  export default nextConfig;
  